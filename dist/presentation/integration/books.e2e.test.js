// Integration tests are disabled in CI for faster runs; keep a tiny placeholder so Jest treats this as a valid suite.
test('placeholder integration test - noop', () => {
    expect(true).toBe(true);
});
export {};
