// (Replaced) Keep this file minimal to avoid broad wildcard that makes imports `any`.
// Use `src/types/web-assets.d.ts` for asset/style declarations instead.

// Intentionally left minimal for API-only project.

declare module '@web/*';
